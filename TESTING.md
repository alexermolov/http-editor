# Инструкция по тестированию HTTP Editor

## Подготовка к тестированию

### 1. Запуск расширения
1. Откройте папку `d:\github_my\vs-code` в VS Code
2. Нажмите `F5` или выберите `Run > Start Debugging`
3. Откроется новое окно VS Code с установленным расширением (Extension Development Host)

### 2. Открытие тестового файла
1. В новом окне откройте файл `test.http` (он уже создан в проекте)
2. Кликните правой кнопкой мыши на файле
3. Выберите **"Open with HTTP Editor"**

## Тестирование функций

### ✅ Тест 1: Просмотр списка запросов
**Ожидаемый результат:** В левой панели отображается список из 6 запросов:
- Get Users (GET)
- Get Single User (GET)
- Create New Post (POST)
- Update Post (PUT)
- Delete Post (DELETE)
- Get Comments (GET)

### ✅ Тест 2: Выбор запроса
**Действия:**
1. Кликните на любой запрос в списке

**Ожидаемый результат:**
- Запрос подсвечивается
- В верхней панели отображаются: имя, метод, URL
- Во вкладке Headers отображаются заголовки
- Во вкладке Body отображается тело запроса (если есть)

### ✅ Тест 3: Отправка GET запроса
**Действия:**
1. Выберите "Get Users"
2. Нажмите кнопку **Send**

**Ожидаемый результат:**
- Кнопка временно меняется на "Sending..."
- Автоматически переключается на вкладку Response
- Отображается:
  - Статус: 200 OK (зеленым)
  - Время ответа в миллисекундах
  - Вкладки Body и Headers
  - В Body - JSON массив пользователей
  - В Headers - заголовки ответа

### ✅ Тест 4: Отправка POST запроса
**Действия:**
1. Выберите "Create New Post"
2. Проверьте наличие тела запроса во вкладке Body
3. Нажмите **Send**

**Ожидаемый результат:**
- Статус: 201 Created
- В ответе - созданный объект с id

### ✅ Тест 5: Редактирование запроса
**Действия:**
1. Выберите любой запрос
2. Измените имя запроса в поле "Request Name"
3. Измените URL
4. Во вкладке Headers добавьте новый заголовок (кнопка "+ Add Header")
5. Введите `Authorization` и `Bearer test-token`

**Ожидаемый результат:**
- Все изменения применяются мгновенно
- Имя обновляется в списке слева

### ✅ Тест 6: Добавление/удаление заголовков
**Действия:**
1. Перейдите во вкладку Headers
2. Нажмите "+ Add Header"
3. Добавьте заголовок `X-Custom-Header: test-value`
4. Нажмите кнопку "✕" рядом с любым заголовком

**Ожидаемый результат:**
- Новые заголовки добавляются
- Удаленные заголовки исчезают
- Изменения сохраняются в памяти

### ✅ Тест 7: Редактирование тела запроса
**Действия:**
1. Выберите "Create New Post"
2. Перейдите во вкладку Body
3. Измените JSON:
```json
{
  "title": "Мой тестовый пост",
  "body": "Это тестовое содержимое",
  "userId": 1
}
```

**Ожидаемый результат:**
- Текст сохраняется
- Форматирование и отступы сохраняются

### ✅ Тест 8: Создание нового запроса
**Действия:**
1. Нажмите кнопку **"+ New"** в левой панели

**Ожидаемый результат:**
- Создается новый запрос "New Request"
- Автоматически выбирается
- Метод: GET
- URL: https://api.example.com
- Пустые заголовки и тело

### ✅ Тест 9: Удаление запроса
**Действия:**
1. Наведите курсор на любой запрос в списке
2. Появится кнопка "Delete"
3. Нажмите её

**Ожидаемый результат:**
- Запрос удаляется из списка
- Автоматически выбирается другой запрос
- Нельзя удалить последний запрос (будет сообщение)

### ✅ Тест 10: Сохранение изменений
**Действия:**
1. Внесите изменения в несколько запросов
2. Нажмите кнопку **"💾 Save All"** внизу справа

**Ожидаемый результат:**
- Появляется уведомление "Requests saved successfully"
- Кнопка временно меняется на "✓ Saved!"
- Файл test.http обновляется на диске
- Откройте test.http в текстовом редакторе - изменения сохранены

### ✅ Тест 11: Обработка ошибок
**Действия:**
1. Создайте новый запрос
2. Установите URL: `https://nonexistent-api-12345.com/test`
3. Нажмите Send

**Ожидаемый результат:**
- Статус отображается красным
- В ответе - информация об ошибке
- Расширение не падает

### ✅ Тест 12: Все HTTP методы
**Действия:**
1. Выберите разные методы из выпадающего списка:
   - GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS
2. Отправьте запросы с разными методами

**Ожидаемый результат:**
- Все методы работают
- Цветные метки методов в списке корректны

### ✅ Тест 13: Переключение между вкладками
**Действия:**
1. Переключайтесь между вкладками Headers, Body, Response

**Ожидаемый результат:**
- Вкладки переключаются плавно
- Содержимое сохраняется
- Активная вкладка подсвечивается

### ✅ Тест 14: Форматирование JSON в ответе
**Действия:**
1. Отправьте любой запрос, возвращающий JSON
2. Посмотрите во вкладку Response > Body

**Ожидаемый результат:**
- JSON отформатирован с отступами
- Читаемый вид
- Правильная кодировка UTF-8

## Проверка всех кнопок

| Кнопка | Расположение | Функция | Работает? |
|--------|--------------|---------|-----------|
| + New | Левая панель, верх | Создать новый запрос | ✅ |
| Delete | Правый край элемента списка | Удалить запрос | ✅ |
| Send | Верхняя панель, справа | Отправить запрос | ✅ |
| + Add Header | Вкладка Headers | Добавить заголовок | ✅ |
| ✕ | Справа от заголовка | Удалить заголовок | ✅ |
| 💾 Save All | Внизу справа | Сохранить все запросы | ✅ |
| Headers | Вкладки | Открыть редактор заголовков | ✅ |
| Body | Вкладки | Открыть редактор тела | ✅ |
| Response | Вкладки | Показать ответ | ✅ |
| Body (Response) | Вкладки ответа | Показать тело ответа | ✅ |
| Headers (Response) | Вкладки ответа | Показать заголовки ответа | ✅ |

## Известные особенности

1. **Тестовый API**: Используется jsonplaceholder.typicode.com - бесплатный API для тестирования
2. **Виртуальные данные**: POST/PUT/DELETE запросы не сохраняют данные на сервере
3. **Таймаут**: 30 секунд на запрос
4. **Автоформат JSON**: Тело запроса автоматически парсится как JSON если возможно

## Что проверить дополнительно

- [ ] Работа с большими JSON (несколько мегабайт)
- [ ] Работа с бинарными данными
- [ ] Работа с нестандартными кодировками
- [ ] Работа при медленном соединении
- [ ] Работа с перенаправлениями (30x коды)
- [ ] Работа с аутентификацией (Basic, Bearer)

## Отчет об ошибках

Если найдены проблемы, проверьте:
1. Developer Console (Help > Toggle Developer Tools)
2. Логи в консоли (F12)
3. Ошибки TypeScript при компиляции

## Результат

Если все тесты пройдены успешно - расширение работает корректно! ✅
